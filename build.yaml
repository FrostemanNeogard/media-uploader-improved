---
name: "Media Uploader Improved"
guid: "ec712ab4-8ecb-4d34-8087-09db56f33d44"
version: "1.0.0.0"
targetAbi: "10.10.0.0"
framework: "net8.0"
overview: "Upload media files (individually or in batches) to your Jellyfin server from an interactive UI"
description: >
  A Jellyfin plugin that lets users upload media files directly through a standalone web page with an interactive UI. Configure a base upload path and a set of named
  destination presets; choose a destination (and optional subfolder) per upload. Supports
  multiple files at once and batch-uploading a folder's top-level video files.
category: "General"
owner: "FrostemanNeogard"
artifacts:
- "Jellyfin.Plugin.MediaUploaderImproved.dll"
changelog: >
  - Multiple file uploads and folder (batch) uploads
  - Configurable base upload path with user-defined destination presets
  - Optional subfolder per upload
